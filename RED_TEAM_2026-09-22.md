# RED TEAM BASELINE — 2026-09-22

## Verdict
Today UI remains a prototype and is not ready to move to forecast-engine implementation yet.

## P0 — blocks design approval
1. Weather icon system is inconsistent:
   - Hero uses large CSS weather artwork.
   - Hourly/7-day use dot-* CSS icons.
   - Tomorrow uses tiny-weather CSS artwork.
   - No exact approved 8-state production asset set exists.
2. Obsolete icon preview experiment was still visible in production after rollback. Removed in this red-team pass.
3. Forecast sentence exists in HTML but legacy CSS hides .advisory p. Product brief requires a concise forecast sentence.
4. styles.css contains many accumulated v9-v12 override layers. This creates high regression risk and is a root cause of repeated layout/icon breakage.
5. Temporary 8-weather selector is still production-visible. It is a QA control, not final product UI.

## P1 — must resolve before final Today approval
- Current sample data is static and internally mixed; do not judge algorithm coherence from it.
- Hero/day/tomorrow/week icons must eventually use one exact approved asset family.
- Browser CSS currently disables text-size adjustment and viewport zoom. Root cause of prior Samsung mismatch was device text size 165%; accessibility normalization should be cleaned later.
- Records and actual-feeling sections are duplicate-looking blocks in the current long page and need final hierarchy QA.
- Bottom navigation destinations are visual only.
- Share and feeling actions are visual only.

## LOCK / NO-GO
- Do not build the forecast engine yet.
- Do not recreate the previously approved icon reference from memory in CSS or SVG.
- Do not touch locked Hero layout or existing production icons while solving icon assets.
- Do not call a recreated asset “identical” to a reference image.

## Single-path exit from icon loop
1. Obtain/create the exact 8 final icon assets outside production.
2. Review the actual files at hero and small sizes.
3. Approve those exact files.
4. Only then replace Hero/hourly/tomorrow/7-day references with the same assets.
5. CSS only controls size/position.
6. Run mobile QA at 375/390/430 and Samsung/Chrome defaults.
7. Remove QA weather selector.
8. Final Today visual approval.
9. Then forecast engine starts.

## Protected current baseline
Production HTML commit after cleanup: eb03a0d9757b800e2f66916f225207402759203d
